{
  "name": "closed load stays closed",
  "turns": [
    {"body": "Load is covered", "expect": {"action": ["closed"], "state": "closed"}},
    {"body": "Rate is $5,000 all in", "expect": {"action": ["ignored"], "state": "closed"}}
  ]
}
